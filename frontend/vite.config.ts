import path from 'node:path'
import { fileURLToPath } from 'node:url'
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

const __dirname = path.dirname(fileURLToPath(import.meta.url))

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      // polymarket-ui-sdk package.json points to non-existent entry files.
      // Lock the resolution to the shipped ES build so imports work out of the box.
      'polymarket-ui-sdk': path.resolve(
        __dirname,
        'node_modules/polymarket-ui-sdk/dist/index.es.js',
      ),
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    emptyOutDir: true,
  },
})
